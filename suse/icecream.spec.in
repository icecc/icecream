#
# spec file for package icecream
#
# Copyright (c) 2012 SUSE LINUX Products GmbH, Nuernberg, Germany.
#
# All modifications and additions to the file contributed by third parties
# remain the property of their copyright owners, unless otherwise agreed
# upon. The license for this file, and modifications and additions to the
# file, is the same license as for the pristine package itself (unless the
# license for the pristine package is not an Open Source License, in which
# case the license is the MIT License). An "Open Source License" is a
# license that conforms to the Open Source Definition (Version 1.9)
# published by the Open Source Initiative.

# Please submit bugfixes or comments via http://bugs.opensuse.org/
#
# icecream 0


Name:           icecream
BuildRequires:  gcc-c++
BuildRequires:  libcap-ng-devel
Summary:        For Distributed Compile in the Network
License:        GPL-2.0+ and LGPL-2.1+
URL:            https://github.com/icecc/icecream
Group:          Development/Tools/Building
Requires:       /bin/tar
Requires:       /usr/bin/bzip2
%if 0%{?suse_version}
PreReq:         %fillup_prereq
PreReq:         %insserv_prereq
%endif
Prereq:         /usr/sbin/useradd /usr/sbin/groupadd
Requires:       gcc-c++
Version:        @VERSION@
Release:        1
Source0:        ftp://ftp.suse.com/pub/projects/icecream/icecream-%{version}.tar.bz2
BuildRoot:      %{_tmppath}/%{name}-%{version}-build

%description
icecream is the next generation distcc.

%package -n libicecream-devel
Summary:        For Distributed Compile in the Network
Group:          Development/Tools/Building
Requires:       libstdc++-devel

%description -n libicecream-devel
icecream is the next generation distcc.

%prep
%setup -q
# DO NOT ADD PATCHES without github reference

%build
export CFLAGS="$RPM_OPT_FLAGS"
export CXXFLAGS="$RPM_OPT_FLAGS"
make -f Makefile.cvs
%configure
make %{?jobs:-j %jobs}

%install
make DESTDIR=$RPM_BUILD_ROOT install

%preun
%stop_on_removal icecream

%pre
/usr/sbin/groupadd -r icecc 2> /dev/null || :
/usr/sbin/useradd -r -g icecc -s /bin/false -c "Icecream Daemon" -d /var/cache/icecream icecc 2> /dev/null || :

%if 0%{?suse_version}
%post
%{fillup_and_insserv -n icecream icecream}
%endif

%postun
%restart_on_update icecream
%{insserv_cleanup}

%clean
rm -rf ${RPM_BUILD_ROOT}

%files
%defattr(-,root,root)
%config %_sysconfdir/logrotate.d/icecream
%config %_sysconfdir/init.d/icecream
%_bindir/icecc
%_bindir/icerun
%_sbindir/icecc-scheduler
%_sbindir/iceccd
%_sbindir/rcicecream
%_mandir/man*/*
%_libexecdir/icecream
%if 0%{?suse_version}
%_sysconfdir/sysconfig/SuSEfirewall2.d/services/*
%_localstatedir/adm/fillup-templates/sysconfig.icecream
%endif
%attr(-,icecream,icecream)
%_localstatedir/cache/icecream

%files -n libicecream-devel
%defattr(-,root,root)
%_includedir/icecc
%_libdir/libicecc.*
%_libdir/pkgconfig/icecc.pc

%changelog
